{
  "title": "Sentinel",
  "config": {
    "step": {
      "user": {
        "title": "Sentinel",
        "description": "Monitor the health of your Home Assistant integrations and get notified when something breaks.",
        "data": {
          "grace_period": "Grace period (seconds)",
          "fire_events": "Fire bus events on state changes"
        },
        "data_description": {
          "grace_period": "How long an integration must remain in a failed state before being reported (0 = immediate).",
          "fire_events": "Fire a `sentinel_item_changed` event on the HA event bus whenever an item changes state. Useful for automations."
        }
      }
    },
    "abort": {
      "already_configured": "Sentinel is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Sentinel Options",
        "data": {
          "grace_period": "Grace period (seconds)",
          "fire_events": "Fire bus events on state changes",
          "excluded_entries": "Excluded integrations",
          "extra_entries": "Extra integrations to monitor",
          "ignored_device_sources": "Ignored device sources",
          "ignored_device_ids": "Ignored devices",
          "watch_stopped_addons": "Report stopped add-ons as warning"
        },
        "data_description": {
          "grace_period": "How long an integration must remain in a failed state before being reported (0 = immediate).",
          "fire_events": "Fire a `sentinel_item_changed` event on the HA event bus whenever an item changes state.",
          "excluded_entries": "Integrations to exclude from monitoring.",
          "extra_entries": "Additional integrations to monitor (auto-discovered, system, etc.).",
          "ignored_device_sources": "Integration sources to exclude from device monitoring (e.g. mobile_app, cast).",
          "ignored_device_ids": "Specific devices to exclude from monitoring.",
          "watch_stopped_addons": "When enabled, add-ons that are stopped will be reported as warnings. By default, stopped add-ons are ignored (intentional stop)."
        }
      }
    }
  },
  "entity": {
    "binary_sensor": {},
    "sensor": {
      "problem_count": {
        "name": "Problems"
      }
    }
  }
}
